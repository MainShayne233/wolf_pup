%h1.center Contact
.container.center
	%p If you run into any errors while using Wolfpup are there is a feature such as a certain LaTeX command you would like added, send an e-mail, and your concern will be addressed as soon as possible.

.container.col-md-6.col-md-offset-3
	= form_for @contact do |f|

		= f.label :name
		%br
		= f.text_field :name, required: true

		%br

		= f.label :email
		%br
		= f.email_field :email, required: true

		%br

		= f.label :message
		%br
		= f.text_area :message, as: :text

		.hidden
			= f.label :nickname
			%br
			= f.text_field :nickname, hint: 'leave this field blank'

		= f.submit 'Send Message', class: "btn btn-primary"

