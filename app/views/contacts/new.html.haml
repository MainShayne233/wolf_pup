- provide(:title, "Contact")
%h1.center Contact
.container.center.skinny
	%p If you run into any errors while using Wolfpup, or there is a feature that you would like added, send an e-mail, and your concern(s) will be addressed as soon as possible.

.container.col-md-6.col-md-offset-3
	= form_for @contact do |f|

		= f.label :name
		%br
		= f.text_field :name, required: true

		%br

		= f.label :email
		%br
		= f.email_field :email, required: true

		%br

		= f.label :message
		%br
		= f.text_area :message, as: :text

		.hidden
			= f.label :nickname
			%br
			= f.text_field :nickname, hint: 'leave this field blank'

		= f.submit 'Send Message', class: "btn btn-primary"

